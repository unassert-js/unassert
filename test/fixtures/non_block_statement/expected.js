'use strict';
function add(a, b) {
    if (!isNaN(a)) {
    }
    if (typeof b === 'number') {
    }
    if (typeof a === 'number') {
    } else if (typeof b === 'number') {
    } else {
    }
    return a + b;
}
